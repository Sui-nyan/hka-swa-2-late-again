import { title } from "@/components/primitives";
import {BasicMap} from "@/app/map/components/BasicMap";

export default function ConnectionPage() {
  return (
    <section>
        <h1 className={title()}>Check Connection</h1>
    </section>
  );
}
